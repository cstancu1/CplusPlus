#include <iostream>

using namespace std;

int main()
{
    int a,b,c, varmax;
    cout<<"a="; cin>>a;
    cout<<"b="; cin>>b;
    cout<<"c="; cin>>c;

    if(a>=b && a>=c){
        cout<<"a";
    }
    if(b>=a && b>=c){
        cout<<"b";
    }
    if(c>=a && c>=b){
        cout<<"c";
    }
}
