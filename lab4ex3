#include <iostream>

using namespace std;

int main()
{
    int n, i, j=0;
    cout<<"n=";cin>>n;
    do{
        i = n%10;
        n = n/10;
        if(i%3 == 0){
            j = j + i;
        }
    }
    while(n>=1);
    cout<<j;

}
